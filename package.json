{
  "name": "jwtforge",
  "version": "1.0.0",
  "description": "JWT token vending service for testing with JWKS and OIDC discovery endpoints",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "keywords": ["jwt", "oidc", "oauth2", "jwks", "cloudflare-workers", "testing"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
